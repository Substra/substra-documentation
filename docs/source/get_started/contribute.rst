Contribute
----------

.. todo::

    fill this section

    - to generate the documentation go to `docs/` directory and run:

        .. code-block:: console

            $ make

    - if you wish to rebuild all the files including the example library (it will take longer), run:

        .. code-block:: console

            $ make html

    You can then find all the html files in the `docs/build` directory.

    You can find all the .rst documentation files in `docs/source/`

    .. hint:: give the guidelines on how the Substra documentation should look like
