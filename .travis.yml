language: python

python:
  - '3.7'

cache: pip


branches:
  only:
    - master

script:
  - pip install -r requirements.txt
  - make clean
  - make html
  - cp -R _build/html/* docs/
  - echo "Done"
